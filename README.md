#Ripple account balances script

A script that uses [ripple-lib](https://github.com/ripple/ripple-lib) to aggregate balances by currency for a list of accounts.

### Install
`npm install`

### Run

```shell
node ripple-account-balances.js
```